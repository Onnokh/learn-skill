{
  "name": "learn-skill",
  "version": "0.0.4",
  "description": "Generate reusable skills from a topic",
  "type": "commonjs",
  "main": "dist/cli.js",
  "bin": {
    "learn-skill": "dist/cli.js"
  },
  "scripts": {
    "build": "tsup src/cli.ts --format cjs --dts --out-dir dist --clean",
    "prepack": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "tsup": "^8.4.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.7"
  }
}
